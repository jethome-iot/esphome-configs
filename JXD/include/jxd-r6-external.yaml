# For using components from external github repositories
external_components:
  - source:
      type: git
      url: https://github.com/jethome-iot/esphome
      ref: ${branch_name}
    components: [wifi, ethernet, dallas_temp_searcher, display_menu_base]
